extends layout

block content
  h1= title
  p #{title} Authentication
  p Endpoints:
  p {url}/oauth/auth/facebook
  p {url}/forgot/{email}/user
  p {url}/forgot/{email}/driver
  p {url}/forgot/{email}/restaurant
